--- web/app/layout.tsx
+++ web/app/layout.tsx
@@ -1,13 +1,15 @@
 import type { Metadata } from 'next';
+import { Inter, JetBrains_Mono } from 'next/font/google';
 import './globals.css';
 import Providers from '@/components/providers';
+import RootLayoutWrapper from '@/components/root-layout-wrapper';
+
+const inter = Inter({ subsets: ['latin'], variable: '--font-sans' });
+const jetbrainsMono = JetBrains_Mono({ subsets: ['latin'], variable: '--font-mono' });

 export const metadata: Metadata = {
   title: 'Aegis XDR / RMM',
   description: 'Enterprise monitoring and security platform',
 };

 export default function RootLayout({
@@ -15,9 +17,11 @@
 }: Readonly<{
   children: React.ReactNode;
 }>) {
   return (
-    <html lang="en">
-      <body className="antialiased bg-zinc-950 text-zinc-200 selection:bg-sky-500/30">
+    <html lang="en" className={`${inter.variable} ${jetbrainsMono.variable} dark`}>
+      <body className="antialiased bg-zinc-950 text-zinc-100 font-sans selection:bg-sky-500/30" suppressHydrationWarning>
         <Providers>
-          {children}
+          <RootLayoutWrapper>
+            {children}
+          </RootLayoutWrapper>
         </Providers>
       </body>
